import { HideIfLargerThanParentDirective } from './hide-if-larger-than-parent.directive';

describe('HideIfLargerThanParentDirective', () => {
  it('should create an instance', () => {
    const directive = new HideIfLargerThanParentDirective();
    expect(directive).toBeTruthy();
  });
});
