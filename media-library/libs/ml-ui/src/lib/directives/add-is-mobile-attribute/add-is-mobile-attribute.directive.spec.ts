import { AddIsMobileAttributeDirective } from './add-is-mobile-attribute.directive';

describe('AddIsMobileAttributeDirective', () => {
  it('should create an instance', () => {
    const directive = new AddIsMobileAttributeDirective();
    expect(directive).toBeTruthy();
  });
});
