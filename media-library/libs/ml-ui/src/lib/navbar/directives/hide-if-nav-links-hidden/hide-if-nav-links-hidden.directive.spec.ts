import { HideIfNavLinksHiddenDirective } from './hide-if-nav-links-hidden.directive';

describe('HideIfNavLinksHiddenDirective', () => {
  it('should create an instance', () => {
    const directive = new HideIfNavLinksHiddenDirective();
    expect(directive).toBeTruthy();
  });
});
